ANTHROPIC_API_KEY=sk-ant-your-key-here
PORT=3001

# Local-only escape hatch for Sandy to see the full wireframe (features,
# social-proof, cta, footer) without the paywall, so she can build the paid
# PDF report. NEVER set this in Vercel/Hostinger's environment variables —
# it must not exist anywhere in production, no exceptions.
SKIP_PAYWALL=true
